# 시크릿 — 절대 커밋 금지
.env
.env.local
.env.*.local
secrets.env
*.pem
*.key

# 데이터 — Parquet/DB는 로컬에만
data/store/
*.parquet
*.duckdb
*.duckdb.wal
*.db

# 펀더멘털 스냅샷 CSV는 대용량 — manifest(해시/provenance)만 커밋, CSV는 로컬에만
data/snapshots/*.csv

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# External skill/source checkout used by trading-copilot
financial-services/

# uv
.python-version
# uv.lock is tracked for reproducible CI installs (`uv sync --frozen`)

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Logs
logs/
*.log
.harness/

# Local tool artifacts
.omx/

# 백테스트 결과
results/
backtests/
reports/
out/
